export interface GuestPass {
  guest_pass_id: string;
  pass_id: string;
  guest_id: string;
  title: string;
  quantity: number;
  current_quantity: number;
  price: number;
  currency: string;
  status: string;
}
